{
  "name": "Glob Pattern File Discovery",
  "description": "Use glob patterns to discover and include multiple files in context",
  "tags": [
    "context",
    "glob",
    "file_discovery"
  ],
  "setup": {
    "files": {
      "src/models/user.rs": "pub struct User {\n    pub id: u64,\n    pub name: String,\n}\n",
      "src/models/product.rs": "pub struct Product {\n    pub id: u64,\n    pub name: String,\n    pub price: f64,\n}\n",
      "src/models/order.rs": "pub struct Order {\n    pub id: u64,\n    pub user_id: u64,\n    pub items: Vec<u64>,\n}\n",
      "src/lib.rs": "pub mod models;\n"
    },
    "terminal_size": [
      120,
      40
    ]
  },
  "events": [
    {
      "type": "user_input",
      "data": {
        "text": "What data models exist in src/models/?",
        "submit": true
      }
    },
    {
      "type": "llm_response",
      "verify": {
        "execution": {},
        "context": {
          "min_files": 3,
          "included_files": [
            "src/models/user.rs",
            "src/models/product.rs",
            "src/models/order.rs"
          ]
        }
      },
      "strategy": {
        "type": "once",
        "response": {
          "typescript": [
            "async function agent_code(): Promise<string> {",
            "  return `The src/models/ directory contains three data models: User (with id and name), Product (with id, name, and price), and Order (with id, user_id, and items).`;",
            "}"
          ]
        }
      }
    }
  ],
  "final_verify": {
    "execution": {}
  }
}