{
  "name": "Self-Determination GatherContext Action",
  "description": "Tests TypeScript-based self-determination system returning GatherContext action",
  "tags": [
    "agent",
    "self-determination",
    "typescript",
    "context-gathering"
  ],
  "setup": {
    "files": {},
    "terminal_size": [
      80,
      24
    ]
  },
  "events": [
    {
      "type": "user_input",
      "data": {
        "text": "Fix the bug in the authentication module",
        "submit": true
      }
    },
    {
      "type": "llm_response",
      "id": "task_analysis_after_gather",
      "trigger": {
        "pattern": "Fix the bug",
        "match_type": "contains"
      },
      "response": {
        "typescript": [
          "async function agent_code(): Promise<string> {",
          "  return 'Gathered context about authentication module bug';",
          "}"
        ]
      }
    },
    {
      "type": "llm_response",
      "trigger": {
        "pattern": "authentication module bug",
        "match_type": "contains"
      },
      "response": {
        "typescript": [
          "async function agent_code(): Promise<string> {",
          "  return 'Gathered context about authentication module bug';",
          "}"
        ]
      }
    }
  ],
  "final_verify": {
    "execution": {}
  }
}
